{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "mongodb",
        "host": "${process.env.DATABASE_HOST || 'cluster0-y2jvi.mongodb.net'}",
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "${process.env.DATABASE_NAME || 'strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'thienry'}",
        "password": "${process.env.DATABASE_PASSWORD || 'ZIMW2LPSFJw5FPsE'}"
      },
      "options": {}
    }
  }
}
